# python-file-handling
Files, Exceptions, and Errors in Python. Task 1: Read File and Handle Errors A script that attempts to read `sample.txt`.It prints content if file exists &amp; displays a custom error if file is missing. Task 2: Write and Append Data A script that creates a file named `output.txt`, writes user input to it, appends more text, and displays final content.
